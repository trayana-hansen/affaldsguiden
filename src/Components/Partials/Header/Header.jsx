import { React } from "react";
import "./Header.scss";
import NavBar from "../Nav/NavBar";

const Header = () => {
  return (
    <header>
      <NavBar />
    </header>
  );
};

export default Header;
